% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{orb_map}
\alias{orb_map}
\title{World map layer}
\usage{
orb_map(
  data = NULL,
  values = NULL,
  region_col = "region",
  value_col = NULL,
  fill = "#DEE2E6",
  colour = "#FFFFFF",
  width = 0.4,
  alpha = 1,
  palette = "viridis",
  na_fill = "#E9ECEF",
  ocean = NULL
)
}
\arguments{
\item{data}{Optional polygon data frame with columns \code{long}, \code{lat}, \code{group}
and (optionally) \code{region}. Defaults to \link{world_map}.}

\item{values}{Optional data frame giving one value per region, used to fill
the polygons.}

\item{region_col, value_col}{Column names in \code{values} holding the region name
and the numeric value.}

\item{fill}{Fixed fill colour used when \code{values} is not supplied.}

\item{colour}{Border colour.}

\item{width}{Border width in pixels.}

\item{alpha}{Opacity between 0 and 1.}

\item{palette}{Continuous palette used for the choropleth; see
\code{\link[=orb_palettes]{orb_palettes()}}.}

\item{na_fill}{Fill colour for regions with no value.}

\item{ocean}{Optional colour for the sea drawn behind the land: a disc for
the orthographic globe, the panel background otherwise.}
}
\value{
An object of class \code{orb_layer}.
}
\description{
Draws country polygons from the built-in \link{world_map} dataset, or from a data
frame of your own with \code{long}, \code{lat} and \code{group} columns. The projection is
set with \code{\link[=orb_coord_map]{orb_coord_map()}}; if none is given, an equirectangular projection
is used.
}
\details{
To build a choropleth, supply \code{values}: a data frame with a region column
and a value column, matched to map regions by name.
}
\examples{
orb_worldmap()

vals <- data.frame(region = c("Brazil", "India", "France"), v = c(3, 9, 5))
orb(world_map) + orb_map(values = vals, region_col = "region",
                         value_col = "v") + orb_coord_map("robinson")
}
\seealso{
\code{\link[=orb_coord_map]{orb_coord_map()}}, \link{world_map}
}
