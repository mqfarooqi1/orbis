% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{orb_scale}
\alias{orb_scale}
\alias{orb_scale_colour}
\alias{orb_scale_fill}
\alias{orb_scale_size}
\alias{orb_scale_x}
\alias{orb_scale_y}
\title{Scales}
\usage{
orb_scale_colour(palette = "viridis", limits = NULL, reverse = FALSE)

orb_scale_fill(palette = "viridis", limits = NULL, reverse = FALSE)

orb_scale_size(range = c(2, 14), limits = NULL)

orb_scale_x(limits = NULL, trans = "identity", breaks = NULL, expand = 0.04)

orb_scale_y(limits = NULL, trans = "identity", breaks = NULL, expand = 0.04)
}
\arguments{
\item{palette}{Palette name (see \code{\link[=orb_palettes]{orb_palettes()}}) or a character vector of
colours to interpolate.}

\item{limits}{Numeric length-2 vector giving the data range to map, or
\code{NULL} to use the observed range.}

\item{reverse}{Reverse the direction of the palette.}

\item{range}{Length-2 numeric vector giving the smallest and largest radius
in pixels, for \code{\link[=orb_scale_size]{orb_scale_size()}}.}

\item{trans}{Axis transformation: \code{"identity"} (default), \code{"log10"} or
\code{"sqrt"}.}

\item{breaks}{Numeric vector of axis breaks, or \code{NULL} for automatic.}

\item{expand}{Fraction of the data range added as padding at each end.}
}
\value{
An object of class \code{orb_scale}, to be added to a plot.
}
\description{
Scales control how data values are converted to visual values: which colours
a variable spans, how large points become, and the limits and transformation
of the axes.
}
\examples{
p <- orb(mtcars, x = wt, y = mpg, colour = hp) + orb_points()
p + orb_scale_colour("magma", reverse = TRUE)
p + orb_scale_y(limits = c(10, 35))
}
