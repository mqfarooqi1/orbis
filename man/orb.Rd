% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{orb}
\alias{orb}
\title{Start a plot}
\usage{
orb(data, ...)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Aesthetic mappings, given as \code{channel = column}.}
}
\value{
An object of class \code{orb_spec}.
}
\description{
\code{orb()} creates a plot specification: it records the data and the mapping
from variables to visual channels. Layers, labels, scales and themes are
added with \code{+}. Nothing is drawn until the plot is printed or saved, so a
specification is cheap to build and modify.
}
\details{
Mappings are given as bare column names, in the style of a formula-free
grammar: \code{orb(df, x = wt, y = mpg, colour = cyl)}. The recognised channels
are \code{x}, \code{y}, \code{colour} (or \code{color}), \code{fill}, \code{size}, \code{label}, \code{group} and
\code{tooltip}.
}
\examples{
p <- orb(mtcars, x = wt, y = mpg, colour = cyl) + orb_points()
p
}
\seealso{
\code{\link[=orb_points]{orb_points()}}, \code{\link[=orb_line]{orb_line()}}, \code{\link[=orb_bars]{orb_bars()}}, \code{\link[=orb_area]{orb_area()}},
\code{\link[=orb_map]{orb_map()}}, \code{\link[=orb_save]{orb_save()}}, \code{\link[=orb_interactive]{orb_interactive()}}
}
