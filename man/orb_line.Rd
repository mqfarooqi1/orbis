% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{orb_line}
\alias{orb_line}
\title{Line layer}
\usage{
orb_line(
  ...,
  data = NULL,
  width = 1.8,
  colour = NULL,
  alpha = 1,
  smooth = FALSE
)
}
\arguments{
\item{...}{Aesthetic mappings for this layer only, as \code{channel = column}.}

\item{data}{Optional data frame overriding the plot data.}

\item{width}{Line width in pixels.}

\item{colour}{Fixed colour used when the \code{colour} channel is not mapped.}

\item{alpha}{Opacity between 0 and 1.}

\item{smooth}{Draw a smoothed curve through the points instead of straight
segments (a Catmull-Rom style spline). Default \code{FALSE}.}
}
\value{
An object of class \code{orb_layer}.
}
\description{
Connects observations in the order they appear, optionally split by a
\code{group} or \code{colour} mapping.
}
\examples{
df <- data.frame(t = 1:50, v = cumsum(rnorm(50)))
orb(df, x = t, y = v) + orb_line(width = 2, smooth = TRUE)
}
