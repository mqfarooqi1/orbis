% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{orb_interactive}
\alias{orb_interactive}
\title{Open or write an interactive plot}
\usage{
orb_interactive(
  plot,
  file = NULL,
  width = 820,
  height = 520,
  title = "orbis plot"
)
}
\arguments{
\item{plot}{An \code{orb_spec} object.}

\item{file}{Optional output path. If \code{NULL}, an object suitable for the
RStudio viewer or an R Markdown document is returned instead.}

\item{width, height}{Size in pixels.}

\item{title}{Title of the HTML document.}
}
\value{
If \code{file} is given, the path (invisibly); otherwise an
\code{htmltools} tag object.
}
\description{
Builds a self-contained HTML document containing the plot as inline SVG plus
a small embedded script. The result works offline, needs no JavaScript
library, and supports tooltips, hover highlighting, mouse-wheel zoom,
dragging to pan, double-click to reset, and clicking legend keys to show or
hide series.
}
\examples{
p <- orb(mtcars, x = wt, y = mpg, colour = cyl) + orb_points()
f <- tempfile(fileext = ".html")
orb_interactive(p, file = f)
file.exists(f)
unlink(f)
}
