% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_svg.R
\name{orb_svg}
\alias{orb_svg}
\title{Render a plot as SVG}
\usage{
orb_svg(plot, width = 820, height = 520, interactive = TRUE, id = NULL)
}
\arguments{
\item{plot}{An \code{orb_spec} object.}

\item{width, height}{Size in pixels.}

\item{interactive}{Embed JavaScript for tooltips, hover highlighting, zoom,
pan and legend toggling. Set \code{FALSE} for a static image, for example when
embedding in a document that forbids scripts.}

\item{id}{Element id used by the embedded script; generated if \code{NULL}.}
}
\value{
A character string containing SVG (and, if requested, a wrapping
\code{div} with the script).
}
\description{
Produces a standalone SVG image of a plot. SVG is a vector format, so the
result is resolution independent: it stays sharp at any size or zoom level,
which is what makes it suitable both for the web and for print.
}
\examples{
p <- orb(mtcars, x = wt, y = mpg) + orb_points()
s <- orb_svg(p, interactive = FALSE)
substr(s, 1, 40)
}
\seealso{
\code{\link[=orb_save]{orb_save()}}, \code{\link[=orb_interactive]{orb_interactive()}}
}
